# 订阅配置本地加密实施说明

## 目标

本方案保护 FastCat 客户端在收到并解密订阅后的本地缓存，减少用户直接从应用数据目录读取节点名称、域名、端口和订阅 provider 内容的可能性。

本地加密不替代现有的 `fastcat-v1` 订阅下发加密：两者分别保护不同阶段的数据。

```text
服务端订阅 AES-256-GCM 加密
  → 客户端验证并解密 fastcat-v1 信封
  → 得到 YAML / provider 内容
  → 使用设备独有密钥再次 AES-256-GCM 加密
  → 保存为本地密文缓存
```

## 与现有订阅加密的关系

现有 `fastcat-v1` 协议使用 `FASTCAT_KEY_CURRENT`、`FASTCAT_KEY_NEXT` 解密服务端返回的 AES-256-GCM 信封，用于保护订阅下发过程、发现篡改和阻止明文降级。

本次新增的本地 Vault 使用另一把随机生成的设备密钥，仅用于设备上的缓存文件。两把密钥用途不同，不能互相替代。

## 本地 Vault 设计

实现位于：`lib/security/profile_vault.dart`。

### 设备密钥

首次使用时，客户端生成 32 字节随机密钥，并存入系统安全存储：

| 平台 | 密钥保护机制 |
| --- | --- |
| iOS / macOS | Keychain |
| Android | Android Keystore 支持的安全存储 |
| Windows | Windows 安全存储实现 |
| Linux | Secret Service / 系统安全存储实现 |

密钥不写入 YAML、SharedPreferences、资产文件、日志、Git 仓库或 `--dart-define`。

### 密文格式

每个缓存文件使用 AES-256-GCM 加密，格式如下：

```text
FCATCFG1.<Base64 nonce>.<Base64 ciphertext_and_tag>
```

- nonce：每次写入随机生成的 12 字节值；
- GCM tag：随密文保存，用于认证和防篡改；
- AAD：包含应用标识、文件记录标识和格式版本；
- 同一密文复制到另一台设备、替换到另一份配置或被修改时，解密都会失败。

## 主订阅配置流程

### 保存

旧流程：

```text
下载并解密订阅
→ 校验 YAML
→ profiles/{profileId}.yaml
```

新流程：

```text
下载并解密订阅
→ 校验 YAML
→ ProfileVault AES-256-GCM 加密
→ profiles/{profileId}.fcfg
```

相关入口包括首次订阅下载、普通订阅更新、加密订阅服务、自动更新和后台恢复。

### 读取

所有节点恢复、配置解析、切换配置和 iOS VPN 启动前读取，统一通过 `ProfileVault.readText(profileId)` 解密到内存，不再直接读取 `.yaml` 文件。

### 旧缓存迁移

升级后的启动阶段会后台扫描旧的 `profiles/*.yaml`：

```text
旧 YAML 存在
→ 读取并加密写入 .fcfg
→ 成功后删除旧 YAML
```

单份配置在首次使用时也会执行同样的幂等迁移，避免迁移中断导致旧配置不可用。

## Provider 缓存流程

provider 缓存可能包含节点域名和完整节点参数，因此同样纳入本地保护。

### 文件位置

| 用途 | 路径 |
| --- | --- |
| 历史明文缓存 | `profiles/providers/{profileId}/...` |
| 加密持久缓存 | `profiles/providers-secure/{profileId}/.../*.fcfg` |
| 运行时明文缓存 | 系统临时目录 `fastcat-runtime-providers/{profileId}/...` |

### 核心运行流程

```text
客户端启动 / 应用配置
→ 删除上次异常退出遗留的运行时目录
→ 将 providers-secure 中的密文解密到临时运行目录
→ 将 Mihomo provider path 指向运行时目录
→ 核心读取、下载或更新 provider
→ 将运行时 provider 回写为加密持久副本
→ 正常退出、重启或删除配置时删除运行时明文
```

因此正常使用时，核心仍可以读取标准 provider 文件，不影响离线恢复、自动更新失败保留旧节点等既有能力；长期数据目录中保存的是 `.fcfg` 密文。

如果系统或客户端异常终止，运行时文件可能暂时仍存在于系统临时目录；下次启动会在初始化阶段清除该目录。运行时明文无法完全避免，因为 Mihomo 必须读取实际配置内容。

### 删除与清理

删除配置、退出登录相关的配置清理、账号注销关联清理应删除：

- 主订阅密文 `.fcfg`；
- `providers-secure` 中的加密 provider 缓存；
- 历史 `providers` 明文目录；
- 系统临时目录中的运行时 provider 文件。

## iOS VPN 配置处理

主订阅 YAML 以前会被写入：

- App Group 的 `clash.yaml`；
- `NETunnelProviderProtocol.providerConfiguration`。

两处均为持久化明文位置。本次调整后：

```text
主 App 从 ProfileVault 解密 YAML
→ 仅通过当前 VPN 隧道启动参数或 IPC 传入 Packet Tunnel
→ Packet Tunnel 在内存中交给 ClashCore
```

不再将完整 YAML 写入 App Group 或 VPN 偏好设置。VPN 偏好设置只保留不包含节点内容的格式标记。

## iOS Provider 缓存差异

iOS 的 Packet Tunnel 是独立扩展进程，不能访问主 App 的临时目录和默认 Keychain 项。它使用 App Group 作为 Mihomo 的运行目录。

主订阅 YAML 已按上述方式避免持久化明文；但若要将 **Packet Tunnel 自己生成的 provider 缓存** 也实现为“加密持久化 + 运行时明文”，还需要完成以下原生能力：

1. 为 Runner 与 PacketTunnel 配置同一个 Keychain Access Group；
2. 在 Apple Developer 后台为 App ID / Provisioning Profile 启用并重新生成包含 Keychain Sharing 的签名配置；
3. 主 App 与 PacketTunnel 通过该共享 Keychain 读取同一设备 Vault 密钥；
4. PacketTunnel 在启动时解密 App Group 中的 provider 密文到运行时文件，并在停止或更新后重新加密；
5. 移除 App Group 中所有长期 provider 明文缓存。

这一步不能只在 Dart 层完成，且需要有效的 Apple 签名能力配合；否则会导致扩展无法读取密钥或 VPN 启动失败。

## 安全边界

本方案可以防止直接浏览应用数据目录、复制配置文件到其他设备、简单查看缓存或通过普通文件管理方式获取节点配置。

本方案不能防止拥有设备控制权的用户通过以下方式获取当前实际连接目标：

- 运行时内存分析；
- DNS 查询记录；
- 网络连接和流量分析；
- 已解密配置在核心运行期间的临时访问。

因此，本地加密应与设备绑定、短期订阅 token、访问审计、频率限制和服务端网关策略共同使用。

## 验证清单

- `profiles/` 下不再新增主订阅 `.yaml`，而是 `.fcfg`。
- 修改 `.fcfg` 任意字节后，客户端拒绝加载该配置。
- 将 `.fcfg` 复制到另一台设备后，客户端无法解密。
- 旧 `.yaml` 在首次启动或使用后被迁移并删除。
- 桌面和 Android 的 provider path 指向临时运行目录，不再指向长期 `profiles/providers` 目录。
- 正常退出、重启、删除订阅后，运行时 provider 目录被清除。
- iOS 的 App Group 与 VPN 偏好设置中不再出现 `clash.yaml` 或完整 YAML 配置。
