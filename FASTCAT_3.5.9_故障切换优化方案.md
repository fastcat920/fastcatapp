# FastCat 3.5.9 多源故障切换与可靠性优化方案

## 一、目标

本次优化的核心目标是：只要仍有一个 OSS 配置源、一个业务 API 和一个设备网关可用，客户端就应尽可能维持登录、套餐、订阅、节点加载、设备管理和版本更新等正常能力。

同时保证：

- 单个配置源故障不会阻断其他配置源。
- 单个网关或业务 API 故障能够自动切换。
- 新配置不完整、格式错误或版本倒退时，不污染最后一次可用配置。
- 故障恢复后的回切不会打断正在进行的请求。
- 订阅更新失败时保留原有可用订阅和节点。
- 自动更新信息与业务配置一起可靠缓存。

本阶段不实施配置签名和公钥验签；后续如果需要防止 OSS 内容被篡改，可单独增加配置安全机制。

## 二、整体架构

```text
多个普通 OSS 配置源
        │ 并发获取、独立解析和校验
        ▼
候选配置集合 ── 全部失败 ──> 内置紧急 OSS
        │
        ▼
按 config_version 选择完整且较新的配置
        │
        ├─> 业务 API 列表
        ├─> 设备网关列表
        ├─> 客户端更新版本与下载地址
        └─> 其他远程业务配置
        │
        ▼
当前完整配置缓存 + 上一份完整配置缓存
```

客户端负责在多个设备网关之间切换；每个设备网关负责在多个业务 API 之间切换。两层故障转移相互独立，避免任意一层的单点故障影响整个客户端。

## 三、OSS 配置源策略

### 3.1 普通 OSS 并发获取

- 所有普通 OSS 地址并发请求，不再按顺序串行等待。
- 任意一个源超时、网络错误、内容解密失败、JSON 解析失败或配置校验失败，只淘汰该源，不影响其他源继续参与。
- 在短暂收敛窗口内收集成功结果，再从完整候选配置中选择版本最高的配置。
- 如果多个候选配置版本相同，优先采用上次成功来源，其次按配置源原有优先级选择。

这样即使前两个 OSS 失效，只要第三个 OSS 正常，客户端仍能快速取得配置。

### 3.2 紧急 OSS

- 紧急 OSS 仅在所有普通 OSS 均无法提供完整配置时启用。
- 紧急 OSS 不再由 Supabase 用户标记或全局开关控制。
- 紧急 OSS 只承担最终配置兜底，不参与正常来源竞争。

### 3.3 最后成功来源

- 客户端保存上次成功的 OSS 来源。
- 下次启动时可优先利用该信息优化同版本候选配置的选择。
- 来源偏好只是性能优化，不能阻止其他 OSS 接管。

### 3.4 完整配置缓存

本地同时保留：

- 当前完整配置。
- 上一份完整配置。
- 上次成功的 OSS 来源。

完整配置必须包含至少一个业务 API 地址和一个网关地址，并同时保存版本更新信息。新配置只有完成解密、解析、结构检查和版本检查后，才能替换当前缓存。

如果本次所有远程来源都不可用，则加载最后一次完整缓存；如果当前缓存损坏，可继续尝试上一份完整缓存。

## 四、配置版本规则

OSS 配置使用 `config_version` 作为整份配置的单调递增版本号。

规则如下：

- 修改 OSS 中任何会影响客户端的内容时，都必须提高 `config_version`。
- 高版本完整配置可以替换低版本缓存。
- 低版本配置不得覆盖已经保存的高版本配置，防止旧 OSS 恢复后造成配置回滚。
- 同版本配置默认视为同一发布批次，不应用同版本的新内容覆盖稳定缓存。
- 如果必须回滚业务内容，也应发布一个更高的 `config_version`，而不是降低版本号。

客户端版本号和 `config_version` 是两套独立编号：本次客户端版本为 `3.5.9+8`，OSS 配置版本仍按后台发布次数单独递增。

## 五、客户端到设备网关的故障切换

### 5.1 网关来源

客户端网关只来自：

1. 当前通过校验的 OSS 配置。
2. 最后一次验证成功的完整配置缓存。

不再使用硬编码兜底网关。只要能修改任意一个有效 OSS，就可以动态调整网关列表，无需重新打包客户端。

### 5.2 请求级切换

每次请求维护独立的已尝试网关集合，发生以下情况时可尝试下一个网关：

- 连接失败。
- DNS 或 TLS 建连失败。
- 连接或响应超时。
- 网关返回 502、503、504 等临时基础设施错误。

账号密码错误、权限不足、参数错误、套餐状态等业务响应不视为网关故障，避免错误切换。

### 5.3 安全重试边界

允许跨网关自动重放的请求：

- GET、HEAD、OPTIONS 等幂等读取请求。
- 登录请求。
- 心跳或状态上报等已确认可安全重试的请求。

订单创建、支付、兑换等可能产生重复业务结果的写请求不跨网关自动重放，防止重复扣款或重复下单。

### 5.4 配置切换保护

- 新 OSS 网关列表至少有一个网关验证可用后，才替换旧的最后可用列表。
- 新列表尚未验证成功时，保留旧网关继续服务。
- 切换只作用于后续请求，不中断已经开始的请求。

## 六、设备网关到业务 API 的故障切换

### 6.1 地址来源

- OSS 动态配置是业务 API 地址的主要来源。
- `DG_BUSINESS_BASE_URLS` 或旧的单地址变量仅作为网关首次启动且远程配置、缓存都不可用时的种子配置。
- 远程业务地址通过健康校验后，才替换服务器保存的最后可用地址。

### 6.2 熔断与选择

- 当前业务 API 发生连接失败、超时或临时服务错误时，网关切换到下一个可用地址。
- 故障地址进入冷却状态，避免每个用户请求都重复撞向已知故障端点。
- 业务错误不触发基础设施故障切换。
- 写请求遵守安全重试边界，不因切换产生重复业务操作。

### 6.3 自动回切

- 使用备用地址期间，后台每 30 秒探测更高优先级地址。
- 备用地址至少稳定使用 180 秒后才允许回切。
- 主地址连续 3 次健康检查成功后，恢复为后续请求的首选地址。
- 回切不终止正在处理的请求，因此正常情况下用户不会感知明显网络抖动。

客户端网关层也采用同类的稳定回切原则：故障时快速离开，恢复时谨慎返回。

## 七、订阅和节点列表保护

订阅更新采用两阶段替换：

1. 下载新订阅。
2. 解析并校验配置结构。
3. 应用新配置并确认代理组、节点列表有效。
4. 全部成功后才删除或替换旧配置。

如果下载失败、配置缺少 `proxy-groups`、应用失败或节点列表为空，则回滚并保留原有可用订阅，避免出现“套餐正常但节点列表为空”。

## 八、客户端版本更新提示

更新配置与业务路由属于同一份完整配置，必须一起缓存，包括：

- 各平台最新版本号。
- 下载地址。
- 更新说明。
- 强制更新标记和最低版本要求。

因此，只要任意一个 OSS 能返回完整的新配置，客户端就能取得并保存更新信息。之后即使其他 OSS 失效，客户端启动时仍可通过完整缓存恢复更新红点和更新提示。

自动更新检查需要在远程配置或完整缓存就绪后执行，不能只依赖启动瞬间尚未完成的网络请求。手动检查取得的新版本信息也应写入同一稳定缓存，供下次启动使用。

## 九、多网关部署要求

所有设备网关实例必须使用相同的：

- `DG_POSTGRES_DSN`
- `DG_TOKEN_SECRET`
- OSS 解密密钥配置

会话查询在本机内存未命中时应立即回查 PostgreSQL，保证用户被切换到另一个网关后仍能继续使用已有登录会话。

每个网关还应配置：

- `DG_OSS_XOR_KEY`：与 OSS 配置加密方式匹配。
- `DG_OSS_CACHE_FILE`：指向可持久化路径，服务重启后仍能加载最后完整配置。

新版本设备网关需要部署到所有实例，避免不同网关使用不一致的故障切换和缓存规则。

## 十、验收场景

发布前后至少验证以下场景：

1. 三个普通 OSS 仅第一个可用，客户端可正常登录、加载节点并检查更新。
2. 三个普通 OSS 仅中间或最后一个可用，功能仍正常且不会等待所有超时后才启动。
3. 普通 OSS 全部不可用但紧急 OSS 可用，可以获得完整配置。
4. 所有 OSS 临时不可用时，可通过最后完整缓存启动。
5. 一个 OSS 返回更高版本但内容损坏，不覆盖当前稳定缓存。
6. 旧 OSS 恢复但 `config_version` 更低，不发生配置回滚。
7. 仅一个客户端网关可用时，登录、套餐、订阅和设备管理正常。
8. 当前网关故障时，安全请求自动切换；业务错误不切换。
9. 仅一个业务 API 可用时，每个网关都能正确转发业务请求。
10. 主业务 API 恢复后满足稳定期和连续健康条件才回切。
11. 新订阅内容错误或节点为空时，旧节点仍然保留。
12. OSS 更新客户端版本号并提高 `config_version` 后，启动自动检查和手动检查都能发现新版本。
13. 应用重启且网络不可用时，已保存的新版本红点和提示仍可恢复。

## 十一、发布与回滚注意事项

- 每次修改 OSS 配置都先检查 JSON 完整性，再提高 `config_version` 并发布。
- 不要在多个 OSS 上长期保留内容不同但 `config_version` 相同的配置。
- 发布设备网关前备份现有可执行文件、环境配置和持久化数据。
- 健康检查失败时自动恢复旧可执行文件并重新启动服务。
- 客户端发布后保留上一稳定安装包和对应源码提交，便于紧急回滚。
- 配置安全签名未包含在 3.5.9 范围内，OSS 权限、密钥和发布账号仍需严格管理。
